name: "PROD: Deploy all services i send-in-send-out to PROD environment"

on:
  workflow_dispatch:

jobs:
  deploy-cpa-sync:
    uses: ./cpa-sync-deploy-prod.yaml

  deploy-ebms-send-in:
    uses: ./ebms-send-in-deploy-prod.yaml
