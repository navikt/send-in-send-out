name: "Deploy all services i send-in-send-out to DEV environment"

on:
  workflow_dispatch:

jobs:
  deploy-cpa-sync:
    uses: ./.github/workflows/cpa-sync-deploy-dev.yaml

  deploy-ebms-send-in:
    uses: ./.github/workflows/ebms-send-in-deploy-dev.yaml
